import Home from './containers/home.container'

const routes = [{
    path: '/', component: Home,
    breadcrumb: [
        {
            breadcrumbName: 'Home',
        },
    ],
}]

export default routes
